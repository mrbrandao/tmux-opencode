# tmux-opencode: matrix — black bg, green text (Nerd Font required)
set -g @opencode-tmux-theme "matrix"

# Optional: override individual colour or icon
# set -g @opencode-statusline-model-fg "colour82"
# set -g @opencode-statusline-icon-model "✨"
